{
  "name": "kestrel-automerge-spike",
  "private": true,
  "type": "module",
  "scripts": {
    "wasm": "node build-wasm.mjs",
    "test": "npm run wasm && node interop.mjs && tsc && node browser-test.mjs"
  },
  "devDependencies": {
    "@automerge/automerge": "3.4.1",
    "playwright": "1.61.1",
    "typescript": "5.9.3",
    "vite": "8.1.1"
  }
}
