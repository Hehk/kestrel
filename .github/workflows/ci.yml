name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  frontend:
    name: Frontend / ${{ matrix.name }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Typecheck
            command: npm run typecheck
          - name: Lint
            command: npm run lint
          - name: Knip
            command: npm run knip
          - name: Format
            command: npm run format:check
          - name: Test
            command: npm run test:run
          - name: Build
            command: npm run build

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - run: npm ci
      - run: ${{ matrix.command }}

  backend:
    name: Backend / ${{ matrix.name }}
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Format
            command: cargo fmt -- --check
          - name: Clippy
            command: cargo clippy --locked --all-targets -- -D warnings
          - name: Test
            command: cargo test --locked

    steps:
      - uses: actions/checkout@v6

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - run: ${{ matrix.command }}

  review-spike:
    name: Review core / WASM interoperability
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
          targets: wasm32-unknown-unknown
      - run: cargo install wasm-bindgen-cli --version 0.2.128 --locked
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - run: cargo fmt --manifest-path crates/Cargo.toml --all -- --check
      - run: cargo clippy --locked --manifest-path crates/Cargo.toml --all-targets -- -D warnings
      - run: cargo clippy --locked --manifest-path crates/Cargo.toml -p review-wasm --target wasm32-unknown-unknown -- -D warnings
      - run: npm run review:check

  api-schema:
    name: API schema
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - uses: dtolnay/rust-toolchain@stable

      - run: npm ci
      - run: npm run api:check
